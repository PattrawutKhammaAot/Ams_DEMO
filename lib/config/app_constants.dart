import 'package:flutter/material.dart';

//COLOR
//const colorPrimary = Color(0xFF6F35A5);
//const colorPrimaryLight = Color(0xFFF1E6FF);
const colorGreyLight = Color(0xFFFCF4F4);
const colorGreyDark = Color(0xFF636363);
const colorBar = Colors.black54;
const colorBackMenu = Colors.black54;

const colorBackgroundLight = Color.fromRGBO(247, 248, 250, 1);
const colorBackgroundGray = Color.fromRGBO(247, 247, 250, 1.0);

const colorPrimary = Color.fromRGBO(62, 91, 121, 1.0);
const colorPrimaryDark = Color.fromRGBO(55, 81, 108, 1.0);
const colorPrimaryLight = Color.fromRGBO(111, 132, 153, 1.0);
const colorSecondary = Color.fromRGBO(62, 91, 121, 1.0);
const colorSecondaryDark = Color.fromRGBO(55, 81, 108, 1.0);
const colorSecondaryLight = Color.fromRGBO(111, 132, 153, 1.0);
// const colorSecondaryLight = Color.fromRGBO(55, 81, 108, 1.0);

const colorSuccess = Color.fromRGBO(35, 161, 90, 1.0);
const colorInfo = Color.fromRGBO(33, 150, 243, 1);
const colorWarning = Color.fromRGBO(243, 118, 0, 1);
const colorDanger = Color.fromRGBO(232, 26, 47, 1.0);
const colorLight = Color.fromRGBO(255, 255, 255, 1);
const colorDark = Color.fromRGBO(247, 247, 250, 1);

const colorAccent = Color.fromRGBO(243, 118, 0, 1);

const colorActive = Color.fromRGBO(42, 172, 83, 0.9);
const colorInActive = Color.fromRGBO(182, 182, 182, 1.0);

const colorTitleDark = Colors.black87;
const colorTitleSecondDark = Colors.black38;
const colorTitleLight = Colors.white;
const colorGreenDark = Color(0xFF3AA474);

const colorFacebook = Color.fromRGBO(23, 116, 245, 1.0);
const colorGoogle = Color.fromRGBO(220, 78, 65, 1.0);
const colorLine = Color.fromRGBO(53, 207, 0, 1.0);

///
const colorGreen_Linear1 = Color(0xFF95F093);
const colorGreen_Linear2 = Color(0xFF2EF8AF);
const colorblueAccent = Color(0xFF00A9B6);

const fontFamily = 'Noto';

const assetImage = 'assets/images';
const logoImage = '$assetImage/logoPOA.png';
